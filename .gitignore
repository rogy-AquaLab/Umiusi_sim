# --- Claude Code task request (per-task, not version controlled) ---
# Copy ai/templates/task_request.template.yaml -> ai/task_request.yaml and edit.
ai/task_request.yaml

# --- Claude Code local settings ---
.claude/settings.local.json

# --- Colcon build artifacts (only if a workspace ever nests here) ---
build/
install/
log/

# --- Training artifacts (checkpoints, tensorboard logs, videos) ---
models/

# --- CAD raw meshes (huge: base_link.stl ~52M; sim uses primitives. Keep description/ text) ---
umiusi_model/

# --- MuJoCo runtime log ---
MUJOCO_LOG.TXT

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- Editor / OS ---
.vscode/
.idea/
*.swp
.DS_Store

# --- RL run outputs at repo root (videos, logs, run dirs) ---
*.mp4
*.log

# ML export artifacts
*.onnx
